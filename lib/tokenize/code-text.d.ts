import {Code} from 'dist/character/codes'
import {Resolve, Tokenize} from '../shared-types'

export const tokenize: Tokenize
export const resolveAll: Resolve
export function previous(code: Code): boolean
